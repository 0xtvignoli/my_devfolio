"use client";

import React from "react";

/**
 * Neutralized for the OpenCode "flat-on-cream" system: no atmospheric
 * backgrounds. Kept as an inert no-op so existing imports stay valid.
 */
export function GridBackground(): React.JSX.Element | null {
  return null;
}

export default GridBackground;
