"use client";

import React from "react";

/**
 * Neutralized for the OpenCode "flat-on-cream" system: no gradients, blurs,
 * or atmospheric glows. Kept as an inert no-op so existing imports stay valid.
 */
export function AuroraBackground(): React.JSX.Element | null {
  return null;
}

export default AuroraBackground;
